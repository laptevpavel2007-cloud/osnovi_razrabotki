[tool.poetry]
name = "discount-service"
version = "0.0.1"
description = "Service for calculating discounts and promo codes"
authors = ["Pavel Laptev <laptevpavel2007@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{ include = "app", from = "src" }]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.136.1"
uvicorn = {extras = ["standard"], version = "^0.46.0"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"